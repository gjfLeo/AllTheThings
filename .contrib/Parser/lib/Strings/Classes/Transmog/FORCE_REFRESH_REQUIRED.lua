createLocalizationString({
	readable = "This may require a Force Refresh (SHIFT click) to properly be collected.",
	constant = "FORCE_REFRESH_REQUIRED",
	export = true,
	text = {
		en = [[~"This may require a Force Refresh (" .. SHIFT_KEY_TEXT .. " click) to properly be collected."]],
		de = [[~"Dies benötigt möglicherweise eine erzwungene Aktualisierung (" .. SHIFT_KEY_TEXT .. " click) um ordnungsgemäß als gesammelt zu zählen."]],
		es = [[~"Esto puede requerir un refresco forzado (" .. SHIFT_KEY_TEXT .. " + clic) para coleccionarlo correctamente."]],
		mx = [[~"Esto puede requerir una actualización forzada (" .. SHIFT_KEY_TEXT .. " + click) para detectarlo correctamente."]],
		ru = [[~"Может потребоваться Полное Обновление коллекции (" .. SHIFT_KEY_TEXT .. " клик)."]],
		cn = [[~"这可能需要强制刷新（" .. SHIFT_KEY_TEXT .. "点击）正确已收集。"]],
		tw = [[~"這可能需要強制刷新（" .. SHIFT_KEY_TEXT .. "點擊）正確已收集。"]],
	},
})
