createLocalizationString({
	readable = "Enable this option if you want the 'Sort' operation (SHIFT Right Click) to sort by the total progress of each group (instead of by Name)",
	constant = "SORT_BY_PROGRESS_CHECKBOX_TOOLTIP",
	export = true,
	text = {
		en = [[~"Enable this option if you want the 'Sort' operation (" .. SHIFT_KEY_TEXT .. " Right Click) to sort by the total progress of each group (instead of by Name)"]],
		es = [[~"Activa esta opción si quieres la operación de 'Ordenar' (" .. SHIFT_KEY_TEXT .. " + clic derecho) para ordenar sobre el total de progreso de cada grupo (en vez de por nombre)"]],
		mx = [[~"Activa esta opción si quieres la operación de 'Ordenar' (" .. SHIFT_KEY_TEXT .. " + click derecho) para ordenar sobre el total de progreso de cada grupo (en vez de por nombre)"]],
		fr = [[~"Activer cette option si vous souhaitez trier les groupes (" .. SHIFT_KEY_TEXT .. " + clic droit) par % de progression au lieu de les trier par leur nom."]],
		ru = [[~"Включите данную опцию, если Вы хотите, чтобы операция 'Сортирования' (" .. SHIFT_KEY_TEXT .. " правый клик) сортировала по общему прогрессу каждой группы (вместо 'по имени')."]],
		cn = [[~"如果你希望'排序'操作（" .. SHIFT_KEY_TEXT .. "右键）按每个组的总进度排序（而不是按名称）请启用此选项"]],
		tw = [[~"如果你希望'排序'操作（" .. SHIFT_KEY_TEXT .. "右鍵）按每個組的總進度排序（而不是按名稱）請啟用此選項"]],
	},
})
